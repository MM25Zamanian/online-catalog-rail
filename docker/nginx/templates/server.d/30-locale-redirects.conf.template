# Canonical English path.
location = /en {
  return 301 /;
}

location = /en/ {
  return 301 /;
}

# Unsupported locale prefixes are redirected to English root.
location ~ ^/([a-z]{2})(?:/|$) {
  if ($1 !~ ^(en|tr)$) {
    return 301 /;
  }
}
